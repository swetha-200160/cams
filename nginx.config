events {}

http {
    upstream cams_app {
        server cams-container:8000;
    }

    server {
        listen 80;

        location /cams/ {
            proxy_pass http://cams_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}