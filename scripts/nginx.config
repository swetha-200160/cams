events {}

http {
    server {
        listen 80;

        location /cams/ {
            proxy_pass http://cams-container:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}